package com.chan.revernue.filterapplication.adapter;

import android.content.Context;
import android.support.annotation.Nullable;
import android.support.v7.widget.RecyclerView;
import android.util.AttributeSet;

public class MainAdapter extends RecyclerView {
    public MainAdapter(Context context) {
        super(context);
    }

    public MainAdapter(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public MainAdapter(Context context, @Nullable AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
